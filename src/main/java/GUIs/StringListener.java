package GUIs;

public interface StringListener {

    /**
     * Interface que se encarga de detecta los eventos indicados en las clases que la implementan.
     * @param texto que recibe Texto que se usa.
     */
    public void textoEmitido(String texto);

}
